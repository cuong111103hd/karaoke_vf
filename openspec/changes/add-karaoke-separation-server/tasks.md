## 1. Project Skeleton and Planned File Tree

- [x] 1.1 Create the initial project skeleton and keep implementation scoped to this planned tree:

```text
app/
├── README.md                         # create: project overview, uv commands, quickstart
├── pyproject.toml                    # create: package metadata and dependencies
├── uv.lock                           # create/update: generated by uv sync
├── .env.example                      # create: local configuration template
├── .gitignore                        # update: Python, uv, data, model/cache outputs
├── docs/
│   ├── boilerplate_guide.md          # existing: keep
│   ├── architecture.md               # create: local server + portable pipeline design
│   ├── pipeline.md                   # create: batch pipeline behavior and commands
│   ├── colab.md                      # create: Colab setup and pipeline-only execution
│   └── api.md                        # create: local API endpoints and job states
├── scripts/
│   ├── run_separation.py             # create: local CLI separation service entrypoint
│   ├── colab_run_separation.py       # create: Colab-friendly separation entrypoint
│   └── run_server.py                 # create: local FastAPI server entrypoint
├── src/
│   └── app/
│       ├── __init__.py               # create: package marker/version export
│       ├── config/
│       │   ├── __init__.py           # create
│       │   └── settings.py           # create: env/default paths/model settings
│       ├── services/
│       │   ├── __init__.py           # create: export run_separation
│       │   ├── separation_service.py # create: orchestration for URL -> instrumental
│       │   ├── models.py             # create: SeparationOptions/SeparationResult
│       │   └── errors.py             # create: stage-specific service errors
│       ├── integrations/
│       │   ├── __init__.py           # create
│       │   ├── youtube.py            # create: yt-dlp wrapper
│       │   ├── demucs.py             # create: Demucs command builder/runner
│       │   └── ffmpeg.py             # create: ffmpeg command helpers
│       ├── audio/
│       │   ├── __init__.py           # create
│       │   ├── normalize.py          # create: audio normalization helpers
│       │   └── export.py             # create: instrumental output discovery/export helpers
│       ├── api/
│       │   ├── __init__.py           # create
│       │   ├── app.py                # create: FastAPI app factory
│       │   ├── schemas.py            # create: request/response models
│       │   └── routes/
│       │       ├── __init__.py       # create
│       │       ├── jobs.py           # create: create/status job endpoints
│       │       └── files.py          # create: result file endpoint
│       ├── jobs/
│       │   ├── __init__.py           # create
│       │   ├── models.py             # create: JobStatus/JobRecord
│       │   ├── repository.py         # create: local JSON metadata repository
│       │   ├── manager.py            # create: create/read/update job operations
│       │   └── worker.py             # create: background separation service runner
│       ├── storage/
│       │   ├── __init__.py           # create
│       │   ├── paths.py              # create: workspace/download/output path helpers
│       │   └── local.py              # create: local file serving/storage helpers
│       └── utils/
│           ├── __init__.py           # create
│           └── process.py            # create: subprocess utility wrapper
├── tests/
│   ├── unit/
│   │   ├── test_settings.py          # create
│   │   ├── test_storage_paths.py     # create
│   │   ├── test_demucs_command.py    # create
│   │   ├── test_separation_service.py # create
│   │   └── test_job_repository.py    # create
│   └── integration/
│       ├── test_separation_dry_run.py # create: mocked youtube/demucs flow
│       └── test_api_jobs.py          # create: FastAPI job endpoints with mocked worker
└── data/
    ├── .gitkeep                      # create: keep parent data dir only
    ├── jobs/                         # runtime generated, gitignored
    ├── downloads/                    # runtime generated, gitignored
    ├── outputs/                      # runtime generated, gitignored
    └── cache/                        # runtime generated, gitignored
```

- [x] 1.2 Add `pyproject.toml` with `uv`-managed dependencies for Demucs, yt-dlp, FastAPI, Uvicorn, Pydantic, python-dotenv, pytest, and HTTP/API test utilities.
- [x] 1.3 Add `.env.example` and settings defaults for local data directory, Demucs model name, output format, host, and port.
- [x] 1.4 Update `.gitignore` to exclude virtual environments, Python caches, downloaded media, generated stems, output audio, local metadata, and model/cache directories.

## 2. Core Batch Separation Service

- [x] 2.1 Implement separation service data models for options, stage names, output artifact paths, and structured separation results.
- [x] 2.2 Implement storage path helpers that create deterministic job workspaces under `data/jobs/<job_id>/`.
- [x] 2.3 Implement the YouTube integration wrapper using yt-dlp and store source metadata and source audio in the workspace.
- [x] 2.4 Implement ffmpeg integration helpers and audio normalization/export modules that call commands through a shared subprocess utility.
- [x] 2.5 Implement the Demucs separator wrapper using the active Python executable with `-m demucs` and default `--two-stems=vocals`.
- [x] 2.6 Implement output discovery so the separation service can locate the no-vocals/instrumental artifact from Demucs output directories.
- [x] 2.7 Implement `run_separation(...)` in `app.services.separation_service` to orchestrate download, normalization, separation, output export, timing, and structured error reporting.

## 3. CLI and Colab Entrypoints

- [x] 3.1 Add `scripts/run_separation.py` for local batch execution with a YouTube URL and optional output directory.
- [x] 3.2 Add `scripts/colab_run_separation.py` that calls only `app.services.separation_service` and does not import API/job server modules.
- [x] 3.3 Document local CLI usage with `uv run python scripts/run_separation.py "<youtube-url>"`.
- [x] 3.4 Document Colab usage with `uv sync` and `uv run python scripts/colab_run_separation.py "<youtube-url>"`.

## 4. Local Job Server

- [x] 4.1 Implement job models for queued, running, completed, and failed statuses.
- [x] 4.2 Implement a local JSON job repository for creating, reading, updating, and listing job metadata.
- [x] 4.3 Implement a job manager that assigns job IDs, creates workspaces, and records job transitions.
- [x] 4.4 Implement a background worker that calls `run_separation(...)` and updates job metadata on success or failure.
- [x] 4.5 Implement FastAPI app creation and request/response schemas.
- [x] 4.6 Implement job creation and job status endpoints.
- [x] 4.7 Implement result file access for completed instrumental outputs.
- [x] 4.8 Add `scripts/run_server.py` to start the local server with Uvicorn.

## 5. Documentation

- [x] 5.1 Write `README.md` with project purpose, assumptions, local setup, server command, separation command, and known limitations.
- [x] 5.2 Write `docs/architecture.md` explaining the boundary between server orchestration and reusable separation service.
- [x] 5.3 Write `docs/pipeline.md` explaining the batch separation stages and expected output layout.
- [x] 5.4 Write `docs/colab.md` explaining why Colab uses only the separation service entrypoint and how to run it.
- [x] 5.5 Write `docs/api.md` documenting endpoints, job statuses, and example request/response bodies.

## 6. Tests and Verification

- [x] 6.1 Add unit tests for settings defaults and environment overrides.
- [x] 6.2 Add unit tests for storage path creation and job workspace layout.
- [x] 6.3 Add unit tests for Demucs command construction without running Demucs.
- [x] 6.4 Add unit tests for JSON job repository create/update/read behavior.
- [x] 6.5 Add an integration-style dry-run separation service test with YouTube, ffmpeg, and Demucs calls mocked.
- [x] 6.6 Add API tests for job creation and status retrieval with pipeline execution mocked.
- [x] 6.7 Run `uv sync` and `uv run pytest` to verify the implemented skeleton.
- [x] 6.8 Run a documented smoke command for the separation service if local dependencies and ffmpeg are available; otherwise record the skipped reason.
